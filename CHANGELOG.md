# 変更のきろく

新しいものが上です。日付は「その版を作った日」です。

## 1.0.4-beta（2026-08-22）

**ためしの版です。** 1.0.4-alpha から、中がすっかり変わりました。
いちばん大きいのは、**エディタから WebView2（Edge）を外した**ことです。

### なしスタジオが、Windows 2000 から動くようになりました

これまで、エディタは Windows 10 以降でしか動きませんでした。画面に WebView2 を
使っていたためです。画面を **Win32 と GDI だけ**で書きなおし、外の物には
何も頼らないようにしました。

* ブラウザの部品も、実行時ランタイムも要りません
* Visual Studio の C ランタイムも使わず、Windows に最初から入っている
  `msvcrt.dll` につないでいます（栞と同じやりかたです）
* exe は 32bit になりました。1172 KB から **612 KB** に小さくなっています

> **本物の Windows 2000 では、まだ確かめられていません**（手元に無いためです）。
> 確かめてあるのは、`nashi.dll` と `nashi-studio.exe` のどちらにも
> Windows 2000 に無い API が 1 つも無いことと、ヘッダが名乗る最低 OS が
> 5.00 になっていることです（`node tools\\check-imports.js`）。
> 実機で試された方がいらしたら、ぜひ教えてください。

### 画面のこと

見た目は変わりますが、**できることは変わりません**。

* ブロックをつまむ・つなぐ・すてる。つまむと下のものもついてくる
* 欄の打ちこみ、えらぶ欄（日本語の入力も、これまでどおりです）
* 右の作業だなは 9 つ。ためす／ゴースト／立ち絵／うごき／変数／さがす／
  チェック／書き出し／ヘルプ
* 動いている SSP へ送る（しゃべらせる・イベント・話しかける・入れて動かす）

角丸や影がなめらかでないのは、古い Windows でも同じように出すためです。

### そのほか

* **PNG を読めるようになりました**（inflate から自前）。立ち絵の見本を
  そのまま画面に出せます
* 同梱するソフトウェアが無くなりました。LICENSE から WebView2 の
  BSD 条項を外しています
* テストは 13 本になりました。画面のことも、**窓を出さずに**確かめています

### 直したもの

* 読みこんだゴーストの「しぼり込み」（`filter`）をほどいていなかったので、
  「どちらかのあたまがなでられたとき」が「なでられたとき」になっていました
* `settings` と `meta` の既定値をうめていなかったので、しゃべる間隔を
  書いていないゴーストに「0 秒なのでしゃべりません」と言っていました
* 選択肢の行き先の欄にブロックが入っていると、`[object Object]` と
  出していました

## 未リリース（1.0.4-alpha のあと）

* **ライセンスを付けました**（[`LICENSE`](LICENSE)）。このプロジェクト独自の
  **なしライセンス 1.1** です。だれでも無償で、目的を問わず使えます。
  * **フォークしたものを配るときは、もとになったプロジェクトの場所と作者名を
    書いてください**（README でもアプリのヘルプでも構いません）。
    ゴーストを配るときは、これも含めて**一切要りません**。
  * **書き出したゴーストは、まるごとあなたのものです。** 中に `nashi.dll` が
    入りますが、配るときにライセンス文を同梱する必要も、作者名を書く必要も
    ありません。有償で配ってもかまいません。ゴーストを配る人に手間をかけたくないので、
    そこははっきり免除してあります。
  * **同梱していた WebView2 SDK（BSD 3-Clause）の著作権表示を、配布物に入れました。**
    exe に静的リンクしていたので、配るときに表示が必要でしたが、
    **1.0.3 までの zip には入っていませんでした**。`LICENSE` の後ろに全文を置き、
    `-Release` は `LICENSE` が無いと止まるようにしました。
    （そのあと WebView2 そのものを外したので、いまは同梱するものがありません）
  * README の「WebView2 は MIT」という記述を、正しく BSD 3-Clause に直しました。

## 1.0.4-alpha（2026-08-21）

**ためしの版です。** 中身は大きく変わっていますが、**本物の Windows 2000 では
まだ確かめられていません**（手元に無いためです）。確かめてあるのは

* 出来あがった `nashi.dll` に、Windows 2000 に無い API が 1 つも無いこと
* ヘッダが名乗る最低 OS が 5.00（Windows 2000）になっていること
* Windows 11 の上で、テスト 7 本がすべて通ること

までです。古い Windows や VM で試せた方は、うまくいったかどうか教えてもらえると
たすかります。ふだん使いには 1.0.3 のほうをおすすめします。

* **書き出したゴーストが、Windows 2000 から動くようになりました。**
  SSP は公式に「Windows 2000以降」ですが、これまでの `nashi.dll` は
  **Windows Vista 以降でしか読みこめませんでした**（XP でもだめでした）。
  自分では一度も呼んでいない `FlsAlloc` などの新しい API を、Visual Studio の
  C ランタイムが勝手に持ちこんでいたためです。輸入表は読みこみのときに全部
  そろっている必要があるので、古い Windows では **DLL を開くことすらできません**。
  手元が Windows 11 だと当然動くので、ソースを読んでも気づけませんでした。
  * C ランタイムを外し、**Windows に最初から入っている `msvcrt.dll`** に
    つなぎました。足りないぶんだけ `shiori/src/tinycrt.cpp` で用意しています。
    `strtod` や `sprintf` のような「自分で書くと間違えるもの」は借りものの本物です。
  * `nashi.dll` は **268KB から 134KB** になりました。
  * スタックの見張り（`/GS`）は、自前で用意して**そのまま残して**あります。
  * **`tools/check-imports.js` を足しました。** 出来あがった DLL の輸入表を直に読んで、
    Windows 2000 に無い API が混ざっていたら名指しで止めます。`.\build.ps1 -Test`
    にも入れたので、うっかり戻ることはありません。
  * **なしスタジオ（エディタ）は Windows 10 / 11 のままです。** WebView2 が要るので、
    古い Windows では動きません。ゴーストを作るのは新しい Windows で、
    配ったゴーストが動くのは古い Windows から、という形になります。

## 1.0.3（2026-08-20）

あとから手を入れやすくするための回です。ふるまいは変わりません。

* **プレビューが、栞そのもので動くようになりました。** これまでブロックを動かす規則は
  `ui/js/sim.js`（JavaScript）と `shiori/src/interp.cpp`（C++）に**二重に書いてあり**、
  ブロック 1 つにつき 2 回書く必要がありました。片方だけ直すと、エディタでは思ったとおりなのに
  SSP では違う、という形であとから分かります。
  なしスタジオが `interp.cpp` を 64bit で積むようにして、画面は `POST /api/preview` に
  頼むだけにしました。**JavaScript 版 474 行を消しました。**
  * 消す前に、一致テストで「JavaScript 版・32bit の栞・64bit の栞の 3 つが同じ結果」を
    確かめてあります（29 件すべて一致）。
  * 一致テストはこれから 32bit と 64bit の見くらべです。同じ `interp.cpp` なので、
    `int` と `size_t` の大きさに頼った書きかたを見つける役目になりました。
  * `ui/js/sim.js` は `ui/js/player.js` になりました。残っているのは、さくらスクリプトを
    再生するところ（吹き出しと立ち絵の動き）だけです。
  * **「ためす」は exe の中の栞を呼びます。** `ui/index.html` をブラウザで直接開いた場合は
    動きません（保存や書き出しと同じになりました）。
* **`app.js` を 5 つに分けました。** 1728 行あって、どこに何があるか探すのが大変でした。
  `shell.js`（立ち絵とうごき）/ `search.js`（チェックとさがす）/ `dialog.js`（保存と書き出し）/
  `ssp.js`（SSP 連携）に切り出し、`app.js` は 802 行になりました。
  **関数の中身は 1 行も変えていません**（分割の前後で、消えた行も増えた行もゼロです。
  変わったのは `saveProject(false)` が `N.Dialog.saveProject(false)` になる、といった呼びかただけ）。
* **画面のファイルの点検を足しました**（`node ui\test\modules.js`）。
  `ui/js/*.js` を `index.html` と同じ順に読みこんで、落ちないか・`N.Xxx.yyy()` の呼び先が
  ほんとうにあるかを見ます。**分けたときの移し忘れ**は、これまで起動してそのボタンを
  押すまで分かりませんでした。`index.html` への書き忘れ・消し忘れも見ます。
* **そろい点検を足しました**（`node tools\check-blocks.js`）。ブロックは「定義・プレビュー・
  栞・説明・テスト」の 5 か所に同じことを書きますが、どれか 1 つ忘れても、これまでは
  テストが緑のまま通っていました（誰もためしていないので、誰も気づけません）。
  `ui/js/blocks.js` を正として、ついてきていないファイルを名指しします。ビルドは要りません。
  `.\build.ps1 -Test` では、ビルドより先に走ります。
* **手を入れるときの手引きを足しました**（`docs/maintenance.md`）。ブロックの足しかた、
  テストがそれぞれ何を守っているか、つまずきやすいところ、配りかた。
* **プレビューにも出力の上限（32KB）を付けました。** 栞は前から持っていましたが、
  プレビューには無かったので、長い話だけ、ためすと本番でちがう結果になっていました。
* **「前に出したトーク」を情報ブロックからえらべるようにしました。** 栞も説明も持っていたのに、
  エディタのメニューにだけ載っていませんでした。
* 一致テストがズレたときの表示を、ちがいの前後だけにしました（長い話でも画面が流れません）。
* **GitHub に push したら、テストが勝手に走るようにしました**（`.github/workflows/build.yml`）。
  これまでは手元で `build.ps1 -Test` を打った人にしか走らなかったので、打ち忘れると
  壊れたまま進んでしまいました。

## 1.0.2（2026-08-15）

安全のための直しです（2 回目の点検で見つけたもの）。

* **おそい SAORI とゴーストの終了が重なると、栞が落ちることがありました。**
  「待たない呼び出し」が 5 秒で終わらないとき、栞は待つのをやめて片づけに入りますが、
  そのあとスレッドが**消えたメモリ**と**外された栞じしん**を触っていました。
  分けあう置き場を作り、最後まで残った人が片づけるようにし、走っている間は
  栞が外れないよう押さえるようにしました（`FreeLibraryAndExitThread`）。
  ついでに、終わり際に固まらないようにもしました（返らない SAORI を待ちつづけない）。
* **変数の長さに上限（32KB）を付けました。** 「V を『V と V をつなげる』にする」を
  30 回まわすだけで 1GB になり、栞（32bit）がメモリを使いきって SSP ごと落ちる状態でした。
* テストが増えました。**本物の SAORI（テスト用のおそいもの）**を使って、待つ呼び出し・
  待たない呼び出し・**終了と重なる場面**を確かめます。この場面は、直す前のコードでは
  実際にアクセス違反で落ちることを確認してあります。

## 1.0.1（2026-08-15）

安全のための直しです。ふるまいは変わりません（外から来た変な文字の扱いだけ変わります）。

* **さくらスクリプトの注入を防ぎました。** リンクの URL・音のファイル名・選択肢の見出しと
  行き先に入れた文字から、`[` `]` と改行（行き先はさらに `,`）を落とします。
  ここに「言われたこと」（他のゴーストが決められる文字）をはめ込んでいると、
  `]` でタグを閉じて `\![…]` を続けられてしまう状態でした。
* **外部リンクを渡す先を `http` / `https` だけにしました。** これまでは種類を問わず
  OS に渡していたので、`file://` などでプログラムを起動できる余地がありました
  （画面がリンクを作らないので、まだ踏めない状態ではありました）。
* **画面が外のページに移動しないようにしました。** 移動しようとしたら止めて、
  既定のブラウザに渡します。見た目はなしスタジオのまま中身が別物、を防ぎます。
* **exe の中の API を、画面自身から出た要求だけに絞りました。** これまで GET は
  素通しで、`/api/shell/file?path=` などが外から呼べる形でした。

## 1.0.0（2026-08-15）

### 会話が賢くなりました

* ランダムトークに「**まとまり**」を付けられます（朝のトーク、季節のトーク…）。
  「**◯ のトークをどれかよぶ**」ブロックで、その中から 1 つだけ呼べます。
* 情報ブロックに「**いまごろ**」（朝・昼・夕方・夜）・「**いまの季節**」・
  「**いまの曜日（名前）**」・「**前に出したトーク**」を足しました。
  時間で話を出し分けるのが、条件ブロック 1 つでできます。

### エディタが使いやすくなりました

* **さがす**タブ（Ctrl+F）。セリフ・変数名・トーク名からブロックを探して、その場所へ飛べます。
* ブロックの **コピー／切り取り／はりつけ／複製**（Ctrl+C / X / V / D）。
* ヘルプにキーボードの一覧を足しました。

### お手本ゴースト

「開く」→「**お手本から始める**」で、動くゴーストを読みこめます。

| | |
|---|---|
| あいさつ | 起動・終了・ダブルクリックの基本 |
| 好感度が上がる | なでられると変数がふえて、しゃべる内容が変わる |
| 朝・昼・晩でしゃべり分ける | 「いまごろ」とトークのまとまり |
| 名前をきいて呼ぶ | 入力ボックスと変数 |
| ほかのゴーストと話す | ゴースト間通信のしぼり込みと返事 |

お手本は自動テストで「チェックタブに 1 つも引っかからないこと」を確かめています。

### こまかいところ

* `ui	est\` を exe に埋め込まないようにしました（画面では使わないため）。

## 0.9.0（2026-08-15）

はじめてまとめた版です。ここまでにできたことを並べます。

### 作れるもの

* ブロックを積んで伺か（SSP）のゴーストを作り、**そのまま SSP に入れて動かせます**。
  会話の中身は C++ ではなく `ghost.json` に入り、栞（`nashi.dll`）がそれを読んで動きます。
* 仮の立ち絵（シェル）と、**バルーン（吹き出し）**を自動で作れます。自分で描いた絵に
  差し替えたら、書き出し直しても上書きしません。
* 配布用の `.nar` を作れます。

### ブロック

* セリフ、表情、間（ウェイト）、クリック待ち、選択肢、リンク、音、バルーン切り替え
* 変数、条件分岐、くりかえし、ランダム分岐、トークの呼び出し
* **ゴースト間通信** — 話しかけられたときの反応と、こちらから話しかけること
* **3 人目以降のキャラ**（`\p[n]`）
* **外部モジュール（SAORI）** — 答えを待つ呼び出しと、**待たない呼び出し**の両方
* **うごき（SERIKO）** — こまごとに位置ずらし・重ねかた（`interpolate` や `blend-*`）、
  ほかのうごきを動かす／止める、APNG の差しこみ、そのうごきの間だけの当たり判定
* **ネットワーク更新** — `homeurl` と `updates2.dau` の書き出し、更新のイベント、
  更新をはじめるブロック
* **たずねる**（入力ボックス）— 使う人が書いた文字を、そのまま変数に入れます
* **N 秒後によぶ**、**イベントを自分で起こす**、**交代する**、**ブラウザで開く**

### エディタ

* ためすタブ … プレビュー再生、SSP でしゃべらせる、イベント送信、話しかけてみる、
  いまの変数、ゴーストの記憶を消す、**栞の記録（`nashi_debug.txt`）を読む**
* チェックタブ … 空のセリフ・無い変数・行き先のない選択肢などを教えます
* ゴーストタブ … 立ち絵の割り当て、うごきの編集、バルーン、更新のありか

### 中身の守り

`.\build.ps1 -Test` で 4 つのテストが走ります。

| | |
|---|---|
| 一致テスト | プレビュー（`sim.js`）と栞（`interp.cpp`）の出力が一致するか |
| ふるまいテスト | 栞だけが持っている判断（どれを選ぶか・既定の反応・間引きなど） |
| 書き出しテスト | 書き出したファイルの中身（`surfaces.txt` など）と `updates2.dau` の MD5 |
| エディタテスト | プロジェクトの整形とチェックタブ |

### まだできないこと

* SERIKO の残り（`alternativestart` などのまとめ操作、`scaling` / `insert`、着せ替えの `bind`）
* `updates2.dau` は形式が公開されていないため、公開されている読み取り側の実装に
  合わせて書いています。うまくいかないときは SSP に作り直させてください。
